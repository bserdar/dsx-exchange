# Copyright 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

deployments:
  auth-callout:
    helm:
      values:
        image:
          repository: ${runtime.images.auth-callout.image}
          tag: ${runtime.images.auth-callout.tag}
